# Webserver with nginx rever proxy

## Run 
- Download/Clone the repo
- In the main folder run the command: `docker compose up`
- open a browser and type: `localhost:8080`;
- you should see the following:
```
<h1>Full Cycle Rocks</h1>
A lista of names
```

